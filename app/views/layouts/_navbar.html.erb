<nav class="navbar navbar-expand-lg navbar-dark bg-dark px-5 ">
  <div class="container-fluid d-flex justify-content-between py-2">
    <div>
      <%= link_to 'FarmersMarketApp', root_path, class: 'navbar-brand text-success' %>
    </div>
    <div>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item mx-1">
            <%= link_to t('menu.about'), root_path, class: 'nav-link active' %>
          </li>
          <li class="nav-item mx-1">
            <%= link_to t('menu.choose_products'), root_path, class: 'nav-link active' %>
          </li>
          <% if customer_signed_in? %> 
            <li class="nav-item mx-1">
              <%= link_to t('menu.cart'), root_path, class: 'nav-link active' %>
            </li>
            <li class="nav-item mx-1">
              <%= link_to t('menu.my_profile'), root_path, class: 'nav-link active' %>
            </li>
          <li class="nav-item mx-1">
            <%= link_to t('menu.logout'), destroy_customer_session_path, method: :delete, class: 'nav-link active' %>
          </li>
          <% elsif user_signed_in? %>
          <li>
            <%= link_to 'logout', destroy_user_session_path, method: :delete, class: 'nav-link active' %>
          </li>
          <% else %>
          <li class="nav-item mx-1">
            <%= link_to t('menu.login'), new_customer_session_path, class: 'nav-link active' %>
          </li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</nav>